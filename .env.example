# Spotify — powers the live "Off duty" section (see README "Off duty / Spotify").
# Copy this file to .env.local and fill in real values. Leaving these blank is
# fine: the section falls back to the static list in lib/data.js.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
